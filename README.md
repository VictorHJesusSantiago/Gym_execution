# Gym_execution

Simple computer vision app for gym users.

## Quick start

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
2. Run:
   ```bash
   python app.py
   ```

The app opens your webcam and estimates movement intensity (LOW, MEDIUM, HIGH),
which can help users track workout activity in real time.
