# Versões fixadas (pinned) propositalmente.
# Antes de atualizar: `pip-audit -r requirements.txt` para detectar CVEs;
# conferir nomes no PyPI oficial para evitar typosquatting.
#
# Mudanças relevantes:
# - python-jose removido: CVE-2024-33664/33663 (JWT algorithm confusion) — substituído por PyJWT==2.9.0
# - passlib removido: abandonado desde 2020, incompatibilidade silenciosa com bcrypt>=4.0 — substituído por bcrypt nativo
# - python-json-logger adicionado: logging estruturado JSON (ver app/core/logging.py)

fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.9.2
pydantic-settings==2.3.4
sqlalchemy==2.0.31
alembic==1.13.2
psycopg2-binary==2.9.12
bcrypt==4.2.0
PyJWT==2.9.0
python-multipart==0.0.9
redis==5.0.7
slowapi==0.1.9
python-json-logger==2.0.7

# testes
pytest==8.2.2
httpx==0.27.0
